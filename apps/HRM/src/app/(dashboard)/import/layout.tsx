import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Nhập Dữ Liệu | POD HRM",
}

export default function ImportLayout({ children }: { children: React.ReactNode }) {
  return children
}
