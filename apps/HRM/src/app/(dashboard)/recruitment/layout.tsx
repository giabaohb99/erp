import type { Metadata } from "next"

export const metadata: Metadata = { title: "Tuyển Dụng | POD HRM" }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
