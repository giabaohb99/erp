import type { Metadata } from "next"

export const metadata: Metadata = { title: "Bảng Lương | POD HRM" }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
