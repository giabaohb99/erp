import type { Metadata } from "next"

export const metadata: Metadata = { title: "Biến Động Nhân Sự | POD HRM" }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
