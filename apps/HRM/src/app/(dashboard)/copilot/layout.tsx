import type { Metadata } from "next"

export const metadata: Metadata = { title: "HR Copilot | POD HRM" }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
