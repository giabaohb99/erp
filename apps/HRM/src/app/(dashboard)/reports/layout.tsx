import type { Metadata } from "next"

export const metadata: Metadata = { title: "Đơn Từ | POD HRM" }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
