import type { Metadata } from "next"

export const metadata: Metadata = { title: "Chấm Công | POD HRM" }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
