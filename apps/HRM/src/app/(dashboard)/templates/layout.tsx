import type { Metadata } from "next"

export const metadata: Metadata = { title: "Hồ Sơ Mẫu | POD HRM" }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
